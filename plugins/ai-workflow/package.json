{
  "name": "@crouton-kit/ai-workflow",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "ai-workflow": "./bin/ai-workflow"
  },
  "scripts": {
    "build": "node -e \"require('esbuild').buildSync({entryPoints:['src/cli.ts'],bundle:true,platform:'node',format:'esm',outfile:'bin/ai-workflow',banner:{js:'#!/usr/bin/env node'}})\""
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
