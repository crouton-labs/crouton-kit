---
paths:
  - "**/.claude/.ai/modes/*.md"
  - "**/ai-cli/modes/*.md"
---

When creating or modifying ai-cli mode files, use the `modes-authoring` skill for frontmatter fields, system prompt design, prompt wrappers, and model selection guidance.
