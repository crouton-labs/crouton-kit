{
  "name": "@crouton-kit/capture",
  "version": "1.0.0",

  "bin": {
    "capture": "./bin/capture"
  },
  "scripts": {
    "build": "esbuild src/capture.ts --bundle --platform=node --format=cjs --outfile=bin/capture '--banner:js=#!/usr/bin/env node' && chmod +x bin/capture"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
