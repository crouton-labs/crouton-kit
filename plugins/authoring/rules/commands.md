---
paths:
  - "**/commands/**/*.md"
---

When creating or modifying slash commands, use the `commands-authoring` skill for structure, frontmatter fields, features ($ARGUMENTS, inline bash, file references), and best practices.
