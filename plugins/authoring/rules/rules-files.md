---
paths:
  - ".claude/rules/**/*.md"
  - "**/rules/**/*.md"
---

When creating or modifying rule files, use the `rules-authoring` skill for guidance on path patterns, writing constraints, and when to use rules vs other tools.
